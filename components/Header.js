import React, { useContext } from 'react';

import Link from 'next/link';
const Header = () => {
    return (
        <div>
            Hello from header
        </div>
    );
};

export default Header;